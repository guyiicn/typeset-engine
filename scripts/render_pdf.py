#!/usr/bin/env python3
"""PDF 渲染引擎 — 占位，FinRobot 的 PDF 渲染暂时在 finrobot:v2 容器内完成。"""

def render_pdf(data: dict, output: str, template: str = 'default', theme: str = 'default'):
    """后续迁移 FinRobot 的 PDF 渲染逻辑到这里。"""
    raise NotImplementedError("PDF rendering is currently handled by finrobot:v2 container. Migration pending.")
