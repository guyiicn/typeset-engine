# typeset-engine 非 Docker 部署 — 运行时 Python 依赖
# 版本对齐 typeset-engine:v4 容器实测 (Python 3.10)
# 已剔除 finrobot/akshare/backtrader/chromadb/pyautogen/wkhtmltopdf 等历史包袱

# ── 图表 (chart 功能, 依赖 Chrome) ─────────────────
plotly==6.7.0
kaleido==1.2.0
matplotlib==3.10.8
pillow==12.2.0

# ── PDF / DOCX 渲染 (核心) ────────────────────────
# typst 是 Rust 二进制 (install.sh 处理), weasyprint 走 HTML→PDF
weasyprint==68.1
python-docx==1.2.0
python-pptx==1.0.2
pypdf==6.10.2

# ── AI 配图 / AI PPT (需要 GEMINI_API_KEY) ─────────
google-genai==1.73.1
python-dotenv==1.0.1

# ── CLI / 工具 ────────────────────────────────────
click==8.1.7
